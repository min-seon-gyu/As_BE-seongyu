name: Hello World

on:
  pull_request:
    branches:
      - develop

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Hello world
        run: echo "Hello, world!"
